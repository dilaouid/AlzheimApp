import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    rightButton: {
        minHeight: '100%',
        backgroundColor: 'red'
    },
    leftButton: {
        minHeight: '100%'
    },
});

